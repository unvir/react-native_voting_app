export const getErrorMessage = (state: IGlobalState) => state.participants.errorMessage;
export const getParticipants = (state: IGlobalState) => state.participants.participants;
export const isParticipantsPending = (state: IGlobalState) => state.participants.isParticipantsPending;
